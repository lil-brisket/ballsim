/**
 * Domain view of a league sanity report (machine-readable).
 * Implementation lives in src/simulation/league-sanity.
 */

export type {
  LeagueSanityReport,
  LeagueSanityReportMetadata,
  LeagueSanityWarning,
} from "@/simulation/league-sanity";
