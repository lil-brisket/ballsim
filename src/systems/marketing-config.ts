/** Weeks per year for marketing budget burn. */
export const MARKETING_WEEKS_PER_YEAR = 52;

/** Diminishing-returns scale for awareness gain per dollar spent. */
export const MARKETING_AWARENESS_SCALE = 0.000_002;

/** Maximum awareness gained per weekly spend tranche. */
export const MARKETING_MAX_WEEKLY_AWARENESS_GAIN = 3;

/** Weekly awareness decay toward neutral (50). */
export const MARKETING_AWARENESS_DECAY = 0.02;
