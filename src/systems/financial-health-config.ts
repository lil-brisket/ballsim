/** Runway at or below this (and net burn) is critical. Tuned after Phase 2 harness. */
export const HEALTH_CRITICAL_RUNWAY_WEEKS = 2;

/** Runway at or below this (and net burn) is warning. */
export const HEALTH_WARNING_RUNWAY_WEEKS = 8;

/** Cash covering this many weeks of outflow plus no net burn → healthy. */
export const HEALTH_HEALTHY_LIQUIDITY_WEEKS = 8;
